/**
 * @deprecated Import from `@/lib/packingWindows` instead.
 * Kept for backward compatibility with earlier packing components.
 */
export {
  type PackingShift,
  type PackingWindow,
  SHIFT_LABELS,
  WEEKDAY_LABELS,
  startOfLocalDay,
  toDateKey,
  getMonday,
  getWeekDays,
  getShiftWindow,
  inferPackingFromCreatedAt,
} from "@/lib/packingWindows";
